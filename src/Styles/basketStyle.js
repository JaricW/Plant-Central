const itemStyle = {
  display: "flex",
  width: "80%",
  padding: "10px",
  alignItems: "center",
  justifyContent: "space-around",
  backgroundColor: "white",
  border: "1px solid",
  height: "8vh",
};

const divStyle = {
  width: "50px",
};

const imgStyle = {
  maxHeight: "100%",
};

const buttonStyle = {
  padding: "5px",
  borderRadius: "5px",
};

const titleHolderStyle = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
};

const itemHolderStyle = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  height: "56vh",
  overflow: "scroll",
};

const costHolderStyle = {
  display: "flex",
  gap: "10px",
};

const buttonHolderStyle = {
  display: "flex",
  alignItems: "center",
};

const basketStyle = {
  itemStyle,
  divStyle,
  imgStyle,
  buttonStyle,
  titleHolderStyle,
  itemHolderStyle,
  costHolderStyle,
  buttonHolderStyle,
};

export default basketStyle;
