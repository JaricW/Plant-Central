const layoutStyle = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  padding: "5px",
};

const buttonStyle = {
  borderRadius: "10px",
  padding: "5px",
};

const buttonHolderStyle = {
  display: "flex",
  gap: "5px",
};

const sortProductsStyle = {
  buttonStyle,
  buttonHolderStyle,
  layoutStyle
};

export default sortProductsStyle;
